# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "<%= box_name %>"
  <% if box_url.nil? %># <% end %>config.vm.box_url = "<%= box_url %>"
  config.vm.synced_folder ".", "/vagrant", type: "rsync",
    rsync__exclude: ".git/"
end
